{
  "name": "redux-dynamic-modules-persist",
  "private": true,
  "description": "Extension for redux-dynamic-modules to persist state to local storage",
  "keywords": [
    "react",
    "redux",
    "module",
    "dynamic",
    "persist"
  ],
  "scripts": {
    "build:prod": "lerna run build:prod",
    "build:dev": "lerna run build:dev",
    "publish": "lerna publish",
    "publish:force": "lerna publish --force-publish=*"
  },
  "author": "Vadim Ilinchik",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^6.0.1",
    "prettier": "^2.7.1"
  },
  "dependencies": {
    "redux-dynamic-modules-persist-core": "^0.0.3",
    "redux-dynamic-modules-persist-react": "^0.0.3"
  }
}
